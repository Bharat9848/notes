## Analogy
- `struct` is like pojo classes.
- declaration `struct car *c` where c is a reference/pointer to a car object.
- `int (*css_online)(struct cgroup_subsys_state *css);` define pointer to a function `css_online` which takes `cgroup_subsys_state` as an argument.